


const defaultEditConfig = {
  allowInput: false, // 树是否允许录入，默认不允许
  levelno: -1, // 限制单选只能选第几级  多选只显示第几级   默认为-1 均为底级
  placeholder: '请选择',
  initLoadTree: true // 初始时，自动加载树数据
}

export {
  defaultEditConfig
}
